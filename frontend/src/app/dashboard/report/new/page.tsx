'use client';

import { ReportGeneratorLayout } from "@/domain/dashboard/report/components/ReportGeneratorLayout";

/**
 * 새 보고서 생성 페이지
 * - 실제 UI와 로직은 ReportGeneratorLayout 컴포넌트가 담당합니다.
 */
export default function NewReportPage() {
  return <ReportGeneratorLayout />;
} 